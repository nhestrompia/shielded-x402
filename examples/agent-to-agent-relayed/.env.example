RELAYER_ENDPOINT=http://127.0.0.1:3100
PAYER_PRIVATE_KEY=0x

# Wallet state + pool sync
WALLET_STATE_PATH=./wallet-state.json
SHIELDED_POOL_ADDRESS=0x
WALLET_INDEXER_URL=
POOL_RPC_URL=
POOL_FROM_BLOCK=0

# ERC-8004 discovery
ERC8004_CHAIN_ID=84532
ERC8004_IS_TESTNET=true
ERC8004_TOKEN_ID=
ERC8004_ENVIO_GRAPHQL_URL=
ERC8004_REGISTRY_ADDRESS=
ERC8004_RPC_URL=
ERC8004_SCAN_API_URL=

# Optional direct URL bypasses ERC-8004 lookup when set
TARGET_URL=

# Optional A2A invoke URL override (used after reading the agent card)
A2A_INVOKE_URL=

# Require discovered A2A target to expose a real x402 402 challenge
A2A_REQUIRE_X402=true

# Require auto-discovery to select only endpoints that actually return x402 payment challenges
DISCOVERY_REQUIRE_PAYABLE=true
