{
  "name": "@shielded-x402/demo-api",
  "version": "0.1.0",
  "type": "module",
  "main": "src/run-demo.ts",
  "types": "src/run-demo.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "lint": "tsc -p tsconfig.json --noEmit",
    "demo": "tsx src/run-demo.ts"
  },
  "dependencies": {
    "@shielded-x402/client": "workspace:*",
    "viem": "^2.39.3"
  }
}
